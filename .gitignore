# Secrets — never commit
.env
.env.local

# Local geocode cache (repeat-address lookups; races/candidates data ships in-repo)
data/cache.db
data/geocode_cache.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS
.DS_Store

# Deprecated sqlite artifact (Postgres is the serving layer now)
data/tx/app.db

# Agent workspaces
.codex/
output/

# Local-only competitive analysis and notes; never commit or push
local-only/

# Source spec doc — not project output, keep local only
*.docx
